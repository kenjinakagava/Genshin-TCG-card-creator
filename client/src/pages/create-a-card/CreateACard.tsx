import CharacterCard from "../../components/CharacterCard";
import CreateCard from "../../features/CreateCard/CreateCard";

const CreateACard = () => {
  return (
    <main className="h-[100vh] pt-32 container mx-auto">
      {
        //<CharacterCard card={card}/>
      }
      <CreateCard />
    </main>
  );
};

export default CreateACard;
